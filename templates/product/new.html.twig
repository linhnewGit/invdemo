{% extends 'base.html.twig' %}

{% block title %}Add Product{% endblock %}

{% block body %}
    <h1>Add new Product</h1>

    {{ include('product/_form.html.twig') }}

    <a href="{{ path('app_product_index') }}"><button class="btn">{{ button_label|default('Back to list') }}</button></a>
{% endblock %}
